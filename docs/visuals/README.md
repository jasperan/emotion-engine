# Social simulation lab

## Capture notes

Actual SvelteKit browser captures. The personality network is a conceptual illustration, not live simulation data. The scenario composer retains the existing generation flow.

Web captures use Chromium at 1440 × 1080 (desktop) and 390 × 1080 (mobile), with reduced motion enabled. Screenshots are real rendered interfaces, not image-generated UI mockups. Raster renderer examples retain their native dimensions.

## Verification — 2026-09-14

`cd frontend && npm run check` passed with 0 errors and 1 existing CSS warning; `npm run build` passed. Installed SvelteKit/Svelte version compatibility warnings remain. Offline browser checks exercise scenario suggestions, unavailable-service feedback, and responsive layouts. No inference or database integration was tested.

Only the existing visual surfaces were changed. The screenshots are not evidence of end-to-end service availability, accessibility certification, or production performance.
